opencv-contrib-python>=4.5.0  # OpenCV with CUDA and extra modules support
numpy>=1.20.0
ffmpeg-python>=0.2.0
cupy-cuda11x>=10.0.0  # CuPy for GPU-accelerated NumPy operations (adjust version based on CUDA version)
